const file=document.getElementById('file'),token=document.getElementById('token'),result=document.getElementById('result');document.getElementById('upload').onclick=async()=>{if(!token.value.trim()){result.textContent='Add meg az admin kulcsot.';return;}if(!file.files[0]){result.textContent='Válassz ki egy knowledge.json fájlt.';return;}try{const json=JSON.parse(await file.files[0].text());const r=await fetch('/api/admin/import',{method:'POST',headers:{'Content-Type':'application/json','X-Admin-Token':token.value.trim()},body:JSON.stringify(json)});const data=await r.json();result.textContent=data.ok?`Sikeres frissítés: ${data.items} tudáselem.`:`Hiba: ${data.error||'Ismeretlen hiba'}`;}catch(e){result.textContent='A fájl nem érvényes JSON.';}};