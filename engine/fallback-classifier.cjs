'use strict';
const TYPES=new Set(['knowledge_missing','routing_error','alias_missing','context_missing','confidence_rejected','technical_failure']);
function classifyFallback({routing={},candidateCount=0,question='',state={},technicalFailure=false}={}){if(technicalFailure)return'technical_failure';const reasons=new Set(routing.rejectionReasons||[]),evidence=new Set(routing.evidence||[]);if(reasons.has('missing_product_context')||reasons.has('ambiguous_product_reference')||(/^(es\s+)?(az|ezt|azt|melyiket|hogyan hasznaljam|melle)/i.test(String(question).trim())&&!state.lastMentionedProduct))return'context_missing';if(reasons.has('alias_missing')||evidence.has('alias-suspected'))return'alias_missing';if(reasons.has('routing_error')||evidence.has('downstream-answer-available'))return'routing_error';if(reasons.has('knowledge_missing'))return'knowledge_missing';if(candidateCount>0||['low_query_coverage','low_top_match_margin','domain_mismatch','intent_mismatch','context_mismatch'].some(x=>reasons.has(x)))return'confidence_rejected';return'knowledge_missing';}
function gapDisposition(type){if(!TYPES.has(type))throw new Error('invalid_fallback_type');if(['knowledge_missing','alias_missing','confidence_rejected'].includes(type))return'knowledge_gap';if(type==='context_missing')return'context_diagnostic';if(type==='routing_error')return'routing_diagnostic';return'technical_diagnostic';}
module.exports={TYPES,classifyFallback,gapDisposition};
