'use strict';
const {normalize}=require('./normalizer.cjs');
const HAIR_WASH_TYPES=Object.freeze(['liquid_shampoo','solid_shampoo','shampoo_soap']);
function detectProductTypeConstraint(question){const q=normalize(question);if(/\b(samponszappan\w*|sampon szappan\w*)/.test(q))return'shampoo_soap';if(/\b(szilard sampon\w*|samponrud\w*|shampoo bar\w*)/.test(q))return'solid_shampoo';if(/\b(folyekony sampon\w*|sampon\w*)/.test(q))return'liquid_shampoo';if(/\btusfurdo\w*/.test(q))return'tusfurdo';if(/\bszappan\w*/.test(q))return'szappan';if(/\bkrem\w*/.test(q))return'krem';if(/\bbalzsam\w*/.test(q))return'balzsam';return null;}
function inferredHairType(product){const explicit=product?.productType;if(HAIR_WASH_TYPES.includes(explicit))return explicit;const text=normalize([product?.id,product?.name,product?.title,product?.label,...(product?.category||[])].filter(Boolean).join(' '));if(/\b(samponszappan\w*|sampon szappan\w*)/.test(text))return'shampoo_soap';if(/\b(szilard sampon\w*|samponrud\w*|shampoo bar\w*)/.test(text))return'solid_shampoo';if(/\bsampon\w*/.test(text))return'liquid_shampoo';return null;}
function matchesProductType(product,constraint){if(!constraint)return true;if(HAIR_WASH_TYPES.includes(constraint))return inferredHairType(product)===constraint;const text=normalize([product?.id,product?.name,product?.title,product?.label].filter(Boolean).join(' '));if(constraint==='tusfurdo')return /\btusfurdo\w*/.test(text);if(constraint==='szappan')return /\bszappan\w*/.test(text)&&!inferredHairType(product);if(constraint==='krem')return /\bkrem\w*/.test(text);if(constraint==='balzsam')return /\bbalzsam\w*/.test(text);return false;}
module.exports={HAIR_WASH_TYPES,detectProductTypeConstraint,inferredHairType,matchesProductType};
